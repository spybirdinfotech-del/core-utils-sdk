name: iOS Build - Dev

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  ios_dev:
    name: Build iOS (Dev)
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.35.7'

      - run: flutter clean
      - run: flutter pub get
      - run: flutter analyze || true

      - name: Auto Version Upgrade
        run: |
          PLIST="ios/Runner/Info.plist"
          VERSION=$(/usr/libexec/PlistBuddy -c "Print CFBundleShortVersionString" "$PLIST")
          BUILD=$(/usr/libexec/PlistBuddy -c "Print CFBundleVersion" "$PLIST")
          NEW_BUILD=$((BUILD + 1))

          /usr/libexec/PlistBuddy -c "Set :CFBundleShortVersionString $VERSION" "$PLIST"
          /usr/libexec/PlistBuddy -c "Set :CFBundleVersion $NEW_BUILD" "$PLIST"

      - name: Install CocoaPods dependencies
        run: |
          cd ios
          pod install --repo-update

      - name: Build iOS (Dev)
        run: |
          flutter build ios --release --no-codesign --dart-define=FLAVOR=dev -v

          mkdir -p build/ios/ipa/dev/Payload
          cp -r build/ios/Release-iphoneos/Runner.app build/ios/ipa/dev/Payload/
          cd build/ios/ipa/dev
          zip -r Runner-dev.ipa Payload

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios-dev
          path: build/ios/ipa/dev/Runner-dev.ipa
